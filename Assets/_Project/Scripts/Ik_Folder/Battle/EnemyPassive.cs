/// <summary>
/// 스테이지(보드게임)별 적의 고정 패시브. AIStyle(카드 선택 성향)과는 별개로,
/// 라운드 흐름 자체에 개입하는 규칙이다. StageData에서 선택.
/// </summary>
public enum EnemyPassive
{
    None,
    DavinciCode, // 적이 승부에서 패배하면, 다음에 드로우되는 카드는 뒷면 유지 → 승부에 나오면 공개
    Uno,         // 카드가 리셔플될 때 덱에 있는 고유카드 중 하나를 맨 위로(다음에 뽑히게)
    Bang,        // 플레이어가 10초간 승부하지 않으면 무작위 카드로 강제 승부
}
